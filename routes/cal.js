function cal(a,b,c){

    if(c==1) return a+b; // add
    if(c==2) return a-b; // add
    if(c==3) return a*b; // add
    if(c==4) return a/b; // add
    
}
 
let res = cal(1,2,1);

console.log(res);


// 